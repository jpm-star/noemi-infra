# Motor B — copie pra .env e ajuste. Sem .env, tudo roda em MOCK.
MOCK_MODE=true
MAX_UPLOAD_MB=200

# === Troca mock → Higgsfield REAL (ver README, seção "Ponto de troca") ===
# MOCK_MODE=false
# ANTHROPIC_API_KEY=sk-ant-...
# HIGGSFIELD_MCP_TOKEN=...            # OAuth do operador (JP), concluído 2026-07-21
# HIGGSFIELD_ANTHROPIC_MODEL=claude-opus-4-8

# MOCK_DELAY_S=2          # latência simulada do mock
# NOEMI_DATA_DIR=/root/noemi-infra/data
